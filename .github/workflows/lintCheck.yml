name: lint-check
on:
  push:
    branches:
      - '**'
    tags:
      - '**'
      jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2

    -name: checks by linter
    run: make make-lint
      ls ${{ github.workspace }}
    - run: echo "This job's status is ${{ job.status }}.
